{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "sh -c 'gunicorn application.app:app --bind 0.0.0.0:${PORT:-8000}'"
  }
}
